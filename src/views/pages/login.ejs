<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head", {title: "Login"}) %>
  <body class="body">
    <div>
      <%- include("../partials/header", {showBar: false}) %>
      <main class="main">
        <div class="red rectangle"></div>
        <form class="form-container white" action="/login" method="post">
          <h2 class="h2">¡Hola!</h2>
          <input
            class="input"
            type="email"
            name="user"
            placeholder="Tu email"
          />
          <input
            class="input"
            type="text"
            name="password"
            placeholder="Tu contraseña"
          />
          <button class="button">Iniciar sesión</button>
        </form>

        <div class="question">
          <a href="/register">
            ¿No tienes cuenta? <span class="underline">Regístrate</span>
          </a>
        </div>
      </main>
    </div>
    <%- include("../partials/footer") %>
  </body>
</html>
