<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head", {title: "Registro"}) %>
  <body class="body">
    <div>
      <%- include("../partials/header", {showBar: false}) %>
      <main class="main">
        <div class="red rectangle"></div>
        <form class="form-container white" action="/register" method="post">
          <h2 class="h2">¡Registrate!</h2>
          <input
            class="input"
            type="text"
            name="user"
            placeholder="Tu nombre"
          />
          <input
            class="input"
            type="email"
            name="email"
            placeholder="Tu email"
          />
          <input
            class="input"
            type="text"
            name="password"
            placeholder="Tu contraseña"
          />
          <button class="button">Crear cuenta</button>
        </form>

        <div class="question">
          <a href="/login">
            ¿Ya tienes cuenta? <span class="underline">Inicia Sesión</span>
          </a>
        </div>
      </main>
    </div>
    <%- include("../partials/footer") %>
  </body>
</html>
